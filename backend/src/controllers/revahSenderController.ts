import { Response } from 'express'
import { prisma } from '../lib/prisma'
import { AuthRequest } from '../middleware/auth'
import { encryptSecret } from '../services/secretVault'
const ctx=(req:AuthRequest)=>({clinicId:req.user!.clinicId,tenantId:req.user!.tenantId})
export async function index(req:AuthRequest,res:Response){res.json(await prisma.revahSender.findMany({where:ctx(req),select:{id:true,unitId:true,channel:true,provider:true,label:true,address:true,isDefault:true,isActive:true,settings:true,createdAt:true,updatedAt:true},orderBy:[{channel:'asc'},{label:'asc'}]}))}
export async function upsert(req:AuthRequest,res:Response){const c=ctx(req);const b=req.body;const channel=String(b.channel).toUpperCase(),address=String(b.address);const old=await prisma.revahSender.findUnique({where:{clinicId_channel_address:{clinicId:c.clinicId,channel,address}}});if(b.isDefault)await prisma.revahSender.updateMany({where:{...c,channel},data:{isDefault:false}});const encryptedCredentials=b.credentials?encryptSecret(b.credentials):undefined;const row=old?await prisma.revahSender.update({where:{id:old.id},data:{unitId:b.unitId||null,provider:String(b.provider),label:String(b.label),isDefault:Boolean(b.isDefault),isActive:b.isActive!==false,settings:b.settings,...(encryptedCredentials?{encryptedCredentials}:{})}}):await prisma.revahSender.create({data:{...c,unitId:b.unitId||null,channel,provider:String(b.provider),label:String(b.label),address,isDefault:Boolean(b.isDefault),isActive:b.isActive!==false,settings:b.settings,encryptedCredentials}});res.json({...row,encryptedCredentials:undefined})}
